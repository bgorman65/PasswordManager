// Header file for the encryption functions

void decryptPin(int *pin);
int loginPass(int *decryptedPin, int *encryptedPin);
void encryptPin(int *pin);
void encryptService(int *service);
void decryptService(int *service);